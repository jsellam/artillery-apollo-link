{
  "name": "artillery-apollo-link",
  "version": "1.0.3",
  "description": "Apollo link for Artillery yaml generation",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "postinstall": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@apollo/client": "^3.11.8",
    "graphql": "^16.9.0",
    "yaml": "^2.5.1"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
